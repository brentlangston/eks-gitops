---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: yellow-api
spec:
  replicas: 3
  selector:
    matchLabels:
      app: yellow-api
  template:
    metadata:
      labels:
        app: yellow-api
    spec:
      containers:
      - env:
        - name: msg
          value: This is the YELLOW api!
        image: brentlangston/example-api:latest
        imagePullPolicy: "Always"
        name: yellow-api
        ports:
        - containerPort: 8000
      nodeSelector:
        nodegroup-type: api-workloads
