---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pink-api
spec:
  replicas: 3
  selector:
    matchLabels:
      app: pink-api
  template:
    metadata:
      labels:
        app: pink-api
    spec:
      containers:
      - env:
        - name: msg
          value: This is the PINK api!
        image: brentlangston/example-api:latest
        imagePullPolicy: Always
        name: pink-api
        ports:
        - containerPort: 8000
      nodeSelector:
        nodegroup-type: api-workloads
