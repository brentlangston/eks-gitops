apiVersion: v1
kind: Service
metadata:
  name: "red-api"
  annotations:
    alb.ingress.kubernetes.io/healthcheck-path: /
spec:
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: red-api
